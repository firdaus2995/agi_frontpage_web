version: '3'
services:
  agi-frontpage-web:
    container_name: agi-frontpage-web-app
    image: agi-frontpage-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3033:80'
    networks:
      - avrist-network
networks:
  avrist-network:
    driver: 'bridge'
