version: '3'
services:
  agi-frontpage-web:
    container_name: agi-frontpage-web-app
    image: agi-frontpage-web
    build:
      context: .                          
      dockerfile: Dockerfile  
    ports:
      - "3031:80"
    volumes:
      - ./nginx/conf/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - avrist-network
networks:
  avrist-network:
    driver: 'bridge'